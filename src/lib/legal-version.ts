// Bumped whenever CGU / CGV / privacy / refund policy change in a way
// that requires renewed customer acceptance. The current value is stamped
// into every new order's metadata (`terms_version`, `terms_accepted_at`).
export const LEGAL_VERSION = "2026-07-03";
