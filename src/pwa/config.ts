// Interrupteur central de la couche PWA.
//
// Mis temporairement à `false` pour tester le wrapper Capacitor Android
// sans service worker, sans enregistrement PWA et sans manifest.
// Repasser à `true` (et décommenter le plugin VitePWA dans vite.config.ts)
// pour réactiver la PWA.
export const PWA_ENABLED = false;